# Author: github.com/brendansturges
# Description: getUptime2.ps1 - Queries list of systems for # uptime & returns a csv
# getUptime.ps1 WORKS but will hang on corrupted WMI call and is slow
# getUptime2.ps1 uses jobs so it's quicker but currently non-functional pending further dev time

